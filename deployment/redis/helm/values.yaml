architecture: standalone

auth:
  enabled: true
  password: "ab45f4e2-f7ed-4cf2-a751-ed3f6b29c696"

tls:
  enabled: false
  # autoGenerated: false
  # existingSecret: redis-tls
  # certFilename: tls.crt
  # certKeyFilename: tls.key
  # certCAFilename: tls.crt

master:
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1024Mi

  persistence:
    enabled: true
    size: 128Mi

  extraFlags:
    - "--maxmemory 512mb"
    - "--maxmemory-policy allkeys-lru"
